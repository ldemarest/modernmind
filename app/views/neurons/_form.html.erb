<%= form_for(@neuron) do |f| %>
  <% if @neuron.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@neuron.errors.count, "error") %> prohibited this neuron from being saved:</h2>

      <ul>
      <% @neuron.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
